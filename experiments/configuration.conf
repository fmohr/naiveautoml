[PY_EXPERIMENTER]
provider = sqlite
database = experiments
table = example

keyfields = dataset_id:int, seed:int, metric
dataset_id = 61, 1464, 31, 334, 50
seed = 0
metric = log_loss

n_jobs=1

resultfields = time, learner_class, learner_hps, feature_pre_processor_class, feature_pre_processor_hps, data_pre_processor_class, data_pre_processor_hps, neg_log_loss, roc_auc, new_best, statusml, exception
resultfields.timestamps = false